## 一、项目概述

**CM Invoice Tracking** 是用于跟踪汽车客户（如 BYD、Tesla、BMW 等）发票处理流程的内部系统，覆盖从发票创建、工作包流转，到团队视角监控与数据导出。 
技术栈采用 **Next.js 14(App Router) + FastAPI + SQLModel + SQL Server Express**，前后端通过 REST API 通信，前端以现代 UI 实现 Dashboard 与各管理页面，后端集中实现工作流、进度与到期规则等核心业务逻辑。

- **前端**：`frontend/my-app`，Next.js App Router，当前主要页面（发票 / 客户 / 用户）已接入真实后端 API，Dashboard 仍使用 Mock 数据。
- **后端**：`backend/app`，FastAPI + SQLModel，已实现发票 / 客户 / 用户 / 模板 等核心 API，并完成统一错误格式、CORS 和数据库种子数据。
- **数据库**：SQL Server（本地 `.\SQLEXPRESS`），通过 `SQLModel` 映射业务实体，包含 `Invoice`、`Workpackage`、`Customer`、`DueDateRule`、`CM`(用户) 等表。

---

## 二、代码结构与职责

### 1. 前端（`frontend/my-app`）

- **`app/`**：页面路由（App Router）
  - `app/page.tsx`：Dashboard（Overview），**当前使用 Mock 数据**，尚未接入 `/api/overview/*`。
  - `app/invoice/page.tsx`：发票列表视图，已通过 `invoiceApi.list` 调用后端 `/api/invoices`。
  - `app/invoice/[id]/page.tsx`：发票详情页（配合 `components/invoice-detail-client.tsx`），调用 `/api/invoices/{id}`、`/api/invoices/{id}/workpackages/{wpId}`。
  - `app/invoice/new/page.tsx`：新建发票页面，调用 `invoiceApi.create`。
  - `app/customer/page.tsx`：客户列表页，调用 `customerApi.list`。
  - `app/customer/[id]/page.tsx`、`app/customer/new/page.tsx`：客户详情 / 创建页，调用 `customerApi.getById` / `create` / `update` / `getRules` / `setRules`。
  - `app/user/page.tsx`：用户列表页，调用 `userApi.list`。
  - `app/user/[id]/page.tsx`、`app/user/new/page.tsx`：用户详情 / 创建页，调用 `userApi.getById` / `create` / `update`。

- **`components/`**
  - `sidebar.tsx`：左侧导航，统一布局。
  - `invoice-detail-client.tsx`：发票详情中的工作包列表、Mark as Complete 等交互。
  - `components/ui/*`：Button / Card / Input / Select / Tabs / Checkbox 等基础 UI 组件。

- **`lib/api.ts`**
  - **统一封装 API 调用**，与后端统一错误格式对齐：
    - `invoiceApi`：`list`、`getById`、`create`、`update`、`delete`、`updateWorkpackage`。
    - `customerApi`：`list`、`getById`、`create`、`update`、`delete`、`getRules`、`setRules`。
    - `userApi`：`list`、`getById`、`create`、`update`、`delete`。
    - `templateApi`：`list`。
  - **错误处理**：期望后端返回 `{ code, message, details? }`，前端统一抛出 `Error(message)`，由页面组件展示提示。

### 2. 后端（`backend/app`）

- **入口与基础设施**
  - `main.py`：
    - 创建 FastAPI 应用，挂载路由：`templates`、`customers`、`users`、`invoices`。
    - 启动时 `create_db_tables()` 自动建表。
    - 统一错误处理：
      - `HTTPException` → `{code, message, details}`。
      - `RequestValidationError` → `422` + 详细 `details`。
      - 未捕获异常 → `500`，`details` 在 `DEBUG` 时包含堆栈。
    - CORS：允许 `settings.cors_origins` 指定的前端域名（默认包含 `http://localhost:3000`）。
    - 健康检查：`GET /healthz`。
    - 数据库连通性：`GET /db/ping`。
    - 种子数据初始化：`POST /db/init-seed` 调用 `init_db.init_db()`。
    - APScheduler：按配置定时执行 `jobs/monthly_invoice.py`（自动发票相关任务）。

  - `config.py`：读取 `.env` 中 DSN / CORS_ORIGINS / LOG_LEVEL / 调度开关等。
  - `db.py`：SQL Server 引擎和 Session 管理。

- **模型与枚举**
  - `models.py`
    - `WorkpackageTemplate`：工作包模板（4 个标准步骤），`sequence_order` 唯一。
    - `CM`：用户（CM / LCM），包含 `region`、`role`、`scnx` 约束。
    - `Customer`：客户，关联 `cm_id`、`lcm_id`。
    - `DueDateRule`：客户-模板维度的到期规则（`fixed_day` / `nth_weekday` / `last_day_offset`）。
    - `Invoice`：发票，包含 `bn_release_status`、`progress`、`current_workpackage`、`created_time`。
    - `Workpackage`：工作包（UUID 主键），`invoice_id + template_id` 唯一约束。
  - `enums.py`：`RegionEnum`、`UserRoleEnum`、`RuleTypeEnum` 等枚举，与前端状态映射一致。

  - `schemas.py`：所有 Pydantic 模型：
    - `InvoiceCreate / InvoiceRead / InvoiceUpdate`；
    - `WorkpackageRead / WorkpackageUpdate`；
    - `CustomerCreate / CustomerRead / CustomerUpdate`；
    - `DueDateRuleCreate / DueDateRuleRead`；
    - `UserCreate / UserRead / UserListItem / UserUpdate`；
    - `WorkpackageTemplateCreate / Read / Update`；
    - `PageResponse[T]` 分页通用结构。

- **业务服务层 (`services/`)**
  - `services/due_date.py`：
    - `DueDateCalculator.calculate_fixed_day` / `calculate_nth_weekday` / `calculate_last_day_offset`：
      - 将规则 (`fixed_day` / `nth_weekday` / `last_day_offset`) 转换为具体 `date`。
  - `services/invoices.py`：
    - 纯函数：
      - `calculate_progress(workpackages, today)`：根据工作包的 `due_date` + `actual_date` 计算 `Normal/Abnormal/Done`，并返回当前工作包名称。
      - `get_current_step(workpackages)`：返回当前应完成步骤号。
      - `can_update_workpackage`：顺序校验函数（**当前在代码中实际未启用**，工作包更新不再阻止“跳步”）。
    - `InvoiceService`：
      - `create_invoice_with_workpackages`：
        - 校验 `cm_id`、`lcm_id` 角色（调用 `UserService.ensure_user_role`）。
        - 按客户 `DueDateRule` + 启用的 `WorkpackageTemplate`，为每张发票生成工作包。
        - 使用 `DueDateCalculator` 计算每个工作包 `due_date`。
        - 调用 `calculate_progress` 生成初始 `progress` / `current_workpackage`。
      - `get_invoice_with_workpackages`：返回发票及其工作包列表。
      - `update_invoice`：允许更新 `bn_release_status`、`status_comment` 等字段。
      - `list_invoices`：分页 + 多条件筛选（`created_from/to`、`ile`、`cm`、`status`、`progress`）；`current_step` 参数目前预留（代码中为 `pass`）。
      - `delete_invoice`：删除发票及其关联工作包。
      - `update_workpackage`：
        - 根据 `invoice_id` + `workpackage_id` 找到目标工作包。
        - 更新 `actual_date` / `remark`，并基于 `actual_date` 自动维护 `is_completed`。
        - 重新加载该发票下所有工作包，与模板 `sequence_order` 组合调用 `calculate_progress`，更新 `Invoice.progress` / `current_workpackage`。
        - **注意**：顺序校验逻辑在当前实现中被移除，允许跳步更新，和早期测试文档略有出入。

  - `services/customers.py`：
    - `CustomerService`：
      - 客户分页 `list_paginated`，单条 CRUD。
      - 创建/更新时校验 `cm_id` / `lcm_id` 角色。
      - 规则管理：
        - `get_rules(customer_id)`：按 `template_id` 排序返回规则。
        - `set_rules(customer_id, rules)`：幂等批量更新，按 `template_id` 去重，增改删同步，保证 `(customer_id, template_id)` 唯一。

  - `services/users.py`：
    - 底层基于表 `CM`，对 CM/LCM 做统一封装。
    - `list_paginated`：可按角色筛选，分页返回。
    - `get_user_by_id`：可选按角色过滤。
    - `create_user / update_user / delete_user`：NT Account 唯一性校验，更新时支持更改主键。
    - `ensure_user_role`：在发票 / 客户逻辑中用于强校验角色。

- **路由层 (`routers/`)**
  - `routers/templates.py`：Template API
    - `GET /api/templates`：所有模板，按 `sequence_order` 排序。
    - `GET /api/templates/{id}`：单条。
    - `POST /api/templates`：创建（校验 `sequence_order` 唯一，冲突返回 `409`）。
    - `PUT /api/templates/{id}`：更新信息及顺序号（顺序冲突返回 `409`）。
    - `DELETE /api/templates/{id}`：删除。
  - `routers/customers.py`：Customer + 规则管理
    - `GET /api/customers`：分页列表。
    - `GET /api/customers/{id}`：详情。
    - `POST /api/customers`：创建（主键自增，无 ID 冲突问题）。
    - `PUT /api/customers/{id}` / `DELETE /api/customers/{id}`：更新 / 删除。
    - `GET /api/customers/{id}/rules` / `PUT /api/customers/{id}/rules`：读取 / 幂等批量设置规则。
  - `routers/users.py`：User 管理
    - `GET /api/users`：分页列表，可按 `role`（cm/lcm）筛选。
    - `GET /api/users/{id}`：详情（可选 `role` 参数进一步限制）。
    - `POST /api/users` / `PUT /api/users/{id}` / `DELETE /api/users/{id}`：增改删。
  - `routers/invoices.py`：Invoice + Workpackage
    - `POST /api/invoices`：创建发票并自动生成工作包，返回包含已 enrichment 的工作包列表。
    - `GET /api/invoices`：分页+筛选发票列表，并为每条发票附带工作包（包含模板名和顺序）。
    - `GET /api/invoices/{id}`：发票详情（含工作包 + 模板信息）。
    - `PUT /api/invoices/{id}`：更新发票业务字段。
    - `DELETE /api/invoices/{id}`：级联删除发票及其工作包。
    - `PUT /api/invoices/{id}/workpackages/{wpId}`：更新单个工作包（Mark as Complete/修改备注），完成后自动重算进度。

- **辅助脚本**
  - `init_db.py`：初始化模板 / 用户 / 客户等基础数据。
  - `seed_db.py`、`import_users.py`、`import_customer_rules.py`：从 Excel 导入数据，或快速填充测试环境。

- **测试与文档**
  - `backend/test_invoices_api.md`：发票创建 / 列表 / 详情 / 更新 / 删除 的手工测试脚本与预期。
  - `backend/test_workpackages_api.md`：工作包顺序校验与进度计算测试脚本（注意：文档仍按“强顺序约束”描述，当前代码已放宽顺序校验）。
  - `backend/test_error_handling.md`：统一错误格式与 CORS 测试指南。
  - `backend/app/tests/*.py`：核心纯函数的 pytest 单元测试（如 `test_due_date.py`、`test_progress.py`）。

---

## 三、核心业务功能模块（与产品文档对齐）

### 1. Overview（仪表盘）

- **My View - 个人工作视图**
  - **Abnormal Work List**：展示 BN Release Status 异常或 Progress 为 Abnormal 的发票。
  - **To Do List（7 日内到期）**：展示即将到期的工作包。
  - **当前状态**：
    - UI 已在 `app/page.tsx` 完成（表格、筛选、颜色映射、跳详情链接等）。
    - 数据仍为前端 Mock，**尚未实现后端 `/api/overview/my` 聚合 API**，也未接入 TanStack Query / SWR 等数据层。

- **Admin View - 团队管理视图**
  - **Team Invoice List**：支持 ILE / Region / LCM / CM / Status / Progress / Current Step 筛选，导出 Excel。
  - **快捷筛选按钮**：释放异常 / 进度异常高亮。
  - **当前状态**：
    - UI 已完成（筛选组件、异常按钮、导出按钮样式），数据为本地 `adminViewInvoices` Mock。
    - 后端 `/api/overview/team` / `/api/exports/invoices` 尚未落地，导出按钮未真正调用后端。

### 2. Invoice Management（发票管理）

- **列表页**（`app/invoice/page.tsx`）
  - **已实现功能**：
    - 调用后端 `GET /api/invoices`，支持分页和筛选：
      - `created_from/created_to`（YYYY-MM），`ile`，`cm`，`status`，`progress`。
    - 列出字段：WorkID、Created Time、Region、ILE、CM、BN Release Status、Comment、Progress、Next Workpackage、Next Step Date。
    - 通过 `getNextWorkpackage` 从 `invoice.workpackages` 中自动算出下一工作包名称与日期。
    - 多选删除：前端通过 `invoiceApi.delete` 对选中 ID 执行并行删除，然后刷新列表。
    - 状态映射：将后端 `bn_release_status` 映射为颜色点和 All/Partial/None 文本。

  - **与设计略有差异 / TODO**：
    - `current_step` 作为查询参数在后端 `InvoiceService.list_invoices` 中尚未实现（函数中 `pass`），前端也暂未暴露 step 筛选。
    - BN Release Status 的值域（前端仍有 `Pending` / `Released` 等旧命名）需要与后端枚举完全对齐。

- **详情页**（`app/invoice/[id]/page.tsx` + `invoice-detail-client.tsx`）
  - 展示 Header 卡片（ID、Region、ILE、CM、BN Release Status、Progress Health、Created Time）。
  - 展示 4 个工作包（名称、状态、Due Date、Actual Date、Remark）。
  - **Mark as Complete**：
    - 前端：设置 `actual_date` 为当天 / 用户选择日期，通过 `invoiceApi.updateWorkpackage` 调用后端。
    - 后端：更新工作包并自动重算发票 `progress` + `current_workpackage`。
  - Status Comment 区域：当 BN Release Status 为异常时允许填写说明（通过 `PUT /api/invoices/{id}`）。

### 3. Customer Management（客户管理）

- **列表**（`app/customer/page.tsx`）
  - 从 `customerApi.list` 获取分页数据（`GET /api/customers`）。
  - 支持搜索（customer_name）、Region、SCNx 筛选（Region / SCNx 数据来自 LCM 用户信息）。
  - 列出：Customer round/location（用 `remark` 填充）、ILE Customer 名称、Region、SCNx、CM。
  - 支持多选删除，调用 `customerApi.delete`。

- **详情 / 创建页**
  - 通过 `customerApi.getById`、`create`、`update` 实现基本信息编辑。
  - 使用 `customerApi.getRules` / `setRules` 编辑每个模板的 Due Date 规则。
  - 后端通过 `CustomerService.set_rules` 保证一客一模板一条规则，幂等更新。

### 4. User Management（用户管理）

- **列表页**（`app/user/page.tsx`）
  - 从 `userApi.list` 获取分页列表（`GET /api/users`）。
  - 支持按角色（CM/LCM）、Region、Name 搜索筛选。
  - 支持多选删除（目前前端仅打印 log，未真正调用 `userApi.delete`，属于 TODO）。
- **详情 / 创建页**
  - 使用 `userApi.getById` / `create` / `update` 维护 `name`、`nt_account`、`region`、`role`、`scnx` 等。
  - 后端通过 `UserService` 保证角色、主键唯一性和字段合法性。

### 5. 时间规则与进度计算

- **时间规则引擎**：
  - 存储层：`DueDateRule` 表 + `WorkpackageTemplate` 表。
  - 计算层：`DueDateCalculator`（已单测覆盖）。
  - 应用点：新建发票 `create_invoice_with_workpackages` 时，根据客户规则和发票 `created_time` 自动计算每个工作包的 `due_date`。

- **进度计算（Progress & Current Step）**：
  - 纯函数 `calculate_progress` / `get_current_step`：
    - Done：所有工作包 `actual_date` 非空。
    - Normal：下一个未完成工作包的 `due_date >= today`。
    - Abnormal：下一个未完成工作包的 `due_date < today`。
  - 应用点：
    - 发票创建完工作包后初始化 `progress` & `current_workpackage`。
    - 每次更新工作包（`update_workpackage`）后重新计算。

---

## 四、当前开发进度与状态（按阶段/模块）

### 1. 阶段 A：后端基础与数据层

- **已完成**
  - FastAPI 项目初始化，`/healthz` 与 `/db/ping` 正常。
  - SQL Server 连接配置，与 `.env` 中 `SQLSERVER_DSN` 对齐。
  - 核心模型建表：`WorkpackageTemplate`、`Customer`、`Invoice`、`Workpackage`、`DueDateRule`、`CM`。
  - 种子数据脚本 `init_db.py`、`seed_db.py` 可通过 `POST /db/init-seed` 一键执行。
- **TODO: Region需要跟着Customer配置，而不是跟着CM，当前数据库设计中Customer的Region字段是通过关联的CM获取的，后续需要修改表设计，把Region从CM的属性改成Customer的属性**

### 2. 阶段 B：领域纯函数与规则

- **已完成**
  - Due Date 计算纯函数：`DueDateCalculator` 三种规则均已有单元测试。
  - Progress / Current Step 纯函数：`calculate_progress`、`get_current_step`、`can_update_workpackage`（后者当前未在生产路径启用）。
- **注意**
  - 工作包顺序强制校验在早期设计中存在（见 `task.md` / `test_workpackages_api.md`），当前 `update_workpackage` 实现中顺序校验已移除，业务上允许“跳步”完成；只使用前端的顺序校验 （显示为灰色）

### 3. 阶段 C：后端路由（替换 Mock）

- **已完成**
  - 模板 API：`/api/templates` 全 CRUD，顺序唯一性校验，冲突返回 `409`。
  - 客户 API：`/api/customers` + `/api/customers/{id}` CRUD。
  - 客户规则 API：`/api/customers/{id}/rules` GET/PUT 幂等批量更新。
  - 用户 API：`/api/users` 列表 + `/api/users/{id}` CRUD。
  - 发票 API：
    - `POST /api/invoices`：自动生成工作包。
    - `GET /api/invoices`：分页 + 多条件筛选。
    - `GET /api/invoices/{id}`：详情。
    - `PUT /api/invoices/{id}`：更新业务字段。
    - `DELETE /api/invoices/{id}`：级联删除。
  - 工作包更新 API：
    - `PUT /api/invoices/{id}/workpackages/{wpId}`：支持设置 `actual_date` / `remark`，自动维护 `is_completed` 和发票进度。

- **部分完成 / TODO**
  - `current_step` 筛选参数在后端已预留，但尚未实现查询条件。
  - 概览接口 `/api/overview/my`、`/api/overview/team` 尚未开发，对应的进度/异常聚合仍在设计阶段。
  - 导出接口 `/api/exports/invoices` 尚未实现，前端仅有 Excel 按钮 UI。

- **统一错误 & CORS**
  - 按 `test_error_handling.md` 中的测试用例，404/409/422/500 等统一返回 `{code, message, details}`，CORS 支持 `http://localhost:3000` 已配置完成。

### 4. 阶段 D：前端接入真实 API

- **已完成（已透传真实后端）**
  - 发票模块：
    - 列表：`app/invoice/page.tsx` → `invoiceApi.list`。
    - 详情：`[id]/page.tsx` & `invoice-detail-client.tsx` → `invoiceApi.getById` / `updateWorkpackage` / `update` / `delete`。
    - 新建发票：`invoice/new/page.tsx` → `invoiceApi.create`。
  - 客户模块：
    - 列表：`customer/page.tsx` → `customerApi.list`。
    - 详情/创建：读取/保存客户及规则 → `customerApi.getById` / `create` / `update` / `getRules` / `setRules`。
  - 用户模块：
    - 列表：`user/page.tsx` → `userApi.list`。
    - 详情/创建：`userApi.getById` / `create` / `update`。

- **部分完成 / 未接入**
  - Overview Dashboard：`app/page.tsx` 仍使用 Mock 数据，尚无对 `/api/overview/my` / `/api/overview/team` 的调用。
  - 导出：Invoice / Customer 页面上的 “Export to Excel” 按钮未连后端导出接口（后端尚未实现）。
  - User 页面 Delete：前端 Delete 按钮当前只做 `console.log`，未真正调用 `userApi.delete`。

---

## 五、环境与运行说明（交接重点）

### 1. 本地开发启动

- **后端**
  - 安装依赖：在 `backend` 目录运行  
    `pip install -r requirements.txt`
  - 配置 `.env`（可参考 `architecture.md`/`test_error_handling.md` 中示例），确保：
    - `SQLSERVER_DSN` 指向本机 `.\SQLEXPRESS`。
    - `CORS_ORIGINS=http://localhost:3000`。
  - 启动服务：  
    `uvicorn app.main:app --reload --port 8000`
  - 初始化种子数据（首次）：  
    `curl -X POST http://localhost:8000/db/init-seed`

- **前端**
  - 在 `frontend/my-app` 安装依赖：`npm install` 或 `pnpm install`。
  - 配置 `.env.local`：
    - `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000`
  - 启动开发服务器：`npm run dev`（默认端口 `3000`）。

### 2. 接手时建议的起手动作

1. 按上面步骤本地跑通后端和前端，验证：
   - 发票列表 / 详情 / 工作包更新可用；
   - 客户列表 / 客户规则可编辑；
   - 用户列表 / 用户编辑可用。
2. 参考 `backend/test_invoices_api.md`、`test_workpackages_api.md`、`test_error_handling.md` 按步骤调用关键 API，熟悉数据结构与业务规则。
3. 对照 `doc.md` 与 `architecture.md`，理解产品视角与现有实现差异，尤其是：
   - Dashboard 概览逻辑；
   - 工作包顺序是否应强校验；
   - 导出与团队视图的聚合口径。

---

## 六、后续工作与建议

- **功能补齐**
  - 根据Reigon的业务规则，修改数据库设计
  - 按 `task.md` 阶段 C/D 完成剩余 API：
    - `/api/overview/my`、`/api/overview/team` 聚合接口。
    - `/api/exports/invoices` 导出接口（含 SQL 查询与 Excel 生成）。
  - 前端：
    - Dashboard 改为使用真实 `/api/overview/*` 数据。
    - 完善筛选器的种类
    - 发票 / 客户列表页面的导出按钮接入后端导出 API。
    - User 列表删除操作接入 `userApi.delete`。

这份交接文档可以作为新同事的入口：一方面从产品视角对齐 `doc.md` 中的业务设计，另一方面从代码结构与当前实现情况说明“哪些已经完成、哪些尚在路上”，便于继续迭代。